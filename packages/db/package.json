{
  "name": "@stream-pulse/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "migrate": "tsx src/migrate.ts",
    "lint": "echo 'Lint not configured yet'"
  },
  "dependencies": {
    "@stream-pulse/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*",
    "tsx": "^4.19.2"
  }
}
