{
  "name": "@stream-pulse/session-simulator",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'Lint not configured yet'"
  },
  "dependencies": {
    "@stream-pulse/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "tsx": "^4.19.2",
    "typescript": "*",
    "vitest": "^2.1.8"
  }
}
