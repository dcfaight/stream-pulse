{
  "name": "@stream-pulse/webrtc-sdk",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "echo 'Lint not configured yet'"
  },
  "dependencies": {
    "@stream-pulse/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^2.1.8"
  }
}
